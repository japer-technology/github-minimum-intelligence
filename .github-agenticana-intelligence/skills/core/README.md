# Core Skills

Skills loaded for every specialist agent regardless of domain.

## Capabilities

- **Issue thread reading** — Parse GitHub issue threads to extract context
- **Git operations** — Stage, commit, push with retry-on-conflict
- **Session management** — Create, resume, and persist JSONL session transcripts
- **Comment posting** — Post structured responses as issue comments
- **Prompt extraction** — Strip `~` prefix and extract user intent
- **ReasoningBank access** — Read and write to shared decision memory
